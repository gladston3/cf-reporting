# cf-reporting environment configuration
# Copy to .env.local for development (gitignored)

# === Database ===
DATABASE_URL="postgresql://cf_reporting:cf_reporting@localhost:5432/cf_reporting"

# === Redis ===
REDIS_URL="redis://localhost:6379"

# === Encryption ===
# 32-byte hex key for encrypting Cloudflare API tokens at rest
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="generate-a-32-byte-hex-key-here-do-not-use-this-value"

# === Next.js ===
NEXTAUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="http://localhost:3000"
